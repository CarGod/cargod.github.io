# Repository instructions

## Content maintenance skill

Before creating, editing, translating, or reviewing any file below `content/blog/**` or `content/tutorials/**`, read and follow `skills/luffyliu-site-content-maintainer/SKILL.md` and the references it routes to.

The skill applies only to this repository's versioned content. External `blogs` and GPTPMT repositories are read-only, GPTPMT source material is pinned to commit `e2f47db`, `zh-CN` is the semantic source, and published content clusters must remain synchronized across all six locales. Content changes never imply authorization to commit, push, deploy, or submit URLs externally.
